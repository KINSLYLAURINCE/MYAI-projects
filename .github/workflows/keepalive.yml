name: Keep Render API Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping cats-dogs API
        run: curl -s --max-time 90 https://cats-dogs-api-vkit.onrender.com/health
